apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-llm-summarizer
  labels:
    {{- include "audittrace.labels" . | nindent 4 }}
    app.kubernetes.io/component: llm-summarizer
spec:
  clusterIP: None
  ports:
    - port: {{ .Values.externalLLM.summarizer.port }}
      targetPort: {{ .Values.externalLLM.summarizer.port }}
      protocol: TCP
      name: http
---
apiVersion: v1
kind: Endpoints
metadata:
  name: {{ .Release.Name }}-llm-summarizer
  labels:
    {{- include "audittrace.labels" . | nindent 4 }}
subsets:
  - addresses:
      - ip: {{ .Values.externalLLM.hostIP | default .Values.global.hostNodeIP }}
    ports:
      - port: {{ .Values.externalLLM.summarizer.port }}
        protocol: TCP
        name: http
